import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // All images are self-hosted from /public/images (downloaded from the
  // original WordPress site). No remotePatterns needed.
};

export default nextConfig;
