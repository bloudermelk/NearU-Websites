# dependencies (hoisted by npm workspaces to the repo root, and per-workspace)
node_modules/

# next.js build output, in any site folder
.next/
out/
build/

# env files — never commit credentials (each site has a .env.example instead)
.env*
!.env.example

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts
next-start*.log
